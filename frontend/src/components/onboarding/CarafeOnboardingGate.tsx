/**
 * Placeholder stub — App.tsx references this component but the real one
 * hasn't been authored yet (referenced from commit 30727e9). Renders
 * nothing so the build stays green; replace with the real onboarding-gate
 * logic when ready.
 */
export default function CarafeOnboardingGate() {
  return null;
}
